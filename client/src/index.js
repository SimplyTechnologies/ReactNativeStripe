import React, { Component } from "react";
import PropTypes from "prop-types";
import Stripe from "AppComponents";

class App extends Component {
  render() {
    return <Stripe />;
  }
}

export default App;
